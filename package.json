{
  "name": "urban-garden-service-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:standalone": "node scripts/build-standalone.mjs",
    "build:index3": "node scripts/build-index3-standalone.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "lucide": "^1.21.0"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  }
}
